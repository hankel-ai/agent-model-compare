# Models recognized as Claude (use direct Anthropic API)
claude_models:
  - opus
  - sonnet
  - haiku

# LiteLLM proxy and extra environment variables are configured via .env
# (see .env.example). All variables there are optional — if a variable is
# unset or empty, the related feature is disabled.
